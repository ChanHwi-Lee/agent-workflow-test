import type { ToolDefinition } from "../registry.js";

export const imageEditToolDefinition: ToolDefinition = {
  toolName: "image-edit",
  toolVersion: "v1-stub",
  kind: "asset_prep",
  description: "Edit or refine an existing asset before it is placed on canvas",
};
