import type { ToolDefinition } from "../registry.js";

export const imageGenerateToolDefinition: ToolDefinition = {
  toolName: "image-generate",
  toolVersion: "v1-stub",
  kind: "asset_prep",
  description: "Generate a draft image candidate for a canvas slot",
};
