import type { ToolDefinition } from "../registry.js";

export const graphicCatalogToolDefinition: ToolDefinition = {
  toolName: "graphic-catalog",
  toolVersion: "v1-curated",
  kind: "analysis",
  description: "List and score graphic candidates such as bitmap, vector, illust, or icon",
};
